// This is now handled in the page component with in-memory storage
// Keeping this file as a placeholder for future API expansion

export default function handler(req, res) {
  res.status(200).json({ message: 'API endpoint placeholder' });
}
